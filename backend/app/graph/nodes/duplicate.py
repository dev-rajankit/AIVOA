from app.graph.state import CopilotState

def duplicate_node(state: CopilotState) -> CopilotState:
    """
    Duplicate node for the AIVOA Copilot workflow.
    
    Placeholder for duplicate detection.
    TODO: In future chunks, replace with SQL-based duplicate detection.
    """
    return {
        "duplicate_matches": []
    }
